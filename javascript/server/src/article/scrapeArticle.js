// I imageine doing something like this
// Call this service from the front end with an url as parameter
//
// Send back the meta data and let the front handle the request
//
// We need to find a good html header/meta data parser

// import scrape from 'something-something-parser';
//
// async function scrapeArticle (request, response) {
//   const url = request.url;
//   scrape(url).then( (metaData) => {
//     return metaData
//   });
// }
//
// module.exports = scrapeArticle
